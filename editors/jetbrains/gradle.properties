pluginVersion=0.0.1

# Target platform. CLion is the natural host for a C++ superset, but the
# plugin only uses platform LSP APIs, so IDEA Ultimate also resolves it.
platformType=CL
platformVersion=2024.2

org.gradle.jvmargs=-Xmx2g
org.gradle.caching=true
kotlin.stdlib.default.dependency=false
